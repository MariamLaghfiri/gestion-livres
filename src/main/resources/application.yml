spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gestion-livres
    username: mariam
    password: 0572ml
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  data:
    jpa:
      repositories:
        enabled: true